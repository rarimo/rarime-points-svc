allOf:
  - $ref: '#/components/schemas/FaceEventStateKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        required:
          - fulfilled
        type: object
        properties:
          fulfilled:
            type: bool
            example: true
            description: If face scan event was fulfilled