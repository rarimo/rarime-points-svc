allOf:
  - $ref: '#/components/schemas/CreateBalanceKey'
  - type: object
    x-go-is-request: true
    required:
      - relationships
    properties:
      attributes:
        type: object
        required:
          - referred_by
        properties:
          referred_by:
            type: string
            description: ID of the referrer from the link
            example: "rCx18MZ4"
      relationships:
        type: object
        required:
          - referred_by
        properties:
          referred_by:
            type: object
            required:
              - data
            properties:
              data: 
                $ref: '#/components/schemas/ReferralCodeKey'
                
