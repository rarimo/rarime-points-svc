allOf:
  - $ref: '#/components/schemas/VerifyFaceKey'
  - type: object
    x-go-is-request: true
    required:
      - attributes
    properties:
      attributes:
        required:
          - proof
        type: object
        properties:
          proof:
            type: object
            format: types.ZKProof
            description: |
              Query ZK face verification proof.
              Required for endpoint `/v2/balances/verifyface`.
